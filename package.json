{
  "name": "valentinesday",
  "private": true,
  "scripts": {
    "dev": "cd valentines && npm install && npm run dev",
    "build": "cd valentines && npm install && npm run build",
    "preview": "cd valentines && npm run preview"
  }
}
